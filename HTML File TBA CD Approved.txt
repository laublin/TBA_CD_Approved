<!DOCTYPE html>
<html>
<head>
  <title>Approval</title>
  <script>
    function getParam(param) {
      const urlParams = new URLSearchParams(window.location.search);
      return urlParams.get(param);
    }

    async function triggerFlow() {
      const itemId = getParam("ItemID");

      // ✅ Replace with your actual flow URL
      const response = await fetch("https://prod-xx.logic.azure.com:443/workflows/yourFlowId/triggers/manual/paths/invoke?api-version=2016-10-01&sp=...&sig=...", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ ItemID: itemId })
      });

      document.getElementById("message").innerText = "✅ Customer Data Transformation has been APPROVED!";
    }

    window.onload = triggerFlow;
  </script>
</head>
<body>
  <h2 id="message">Processing approval...</h2>
</body>
</html>
